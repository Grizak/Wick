let sumOdd = fn(upper: i64 = 10): i64 {
	// Test continue — sum all numbers from 1 to upper
	let sum = 0
	for let k = 1; k <= upper; k = k + 1 {
		sum = sum + k
	}
	return sum
}

let sum = sumOdd(20)

exit(sum)